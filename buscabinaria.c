#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int binary_search(int arr[]) {

    int start = 0;
    int last = sizeof(arr) / sizeof(arr[0]);

    return start, last;


}

int main() {

    int arr[] = {1, 2, 3, 4, 5};

    printf(binary_search(arr));

}